<%= form_with(model: faq, local: true) do |form| %>
  <% if faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faq.errors.count, "error") %> prohibited this faq from being saved:</h2>

      <ul>
      <% faq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :leasing %>
    <%= form.text_area :leasing, id: :faq_leasing %>
  </div>

  <div class="field">
    <%= form.label :borrowing %>
    <%= form.text_area :borrowing, id: :faq_borrowing %>
  </div>

  <div class="field">
    <%= form.label :about %>
    <%= form.text_area :about, id: :faq_about %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
